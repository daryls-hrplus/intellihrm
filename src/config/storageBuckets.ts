// Centralized storage bucket IDs used across the app.
// Keep these as constants so we don't hardcode bucket names in many places.

export const ESS_SUPPORTING_DOCUMENTS_BUCKET = "employee-documents";
